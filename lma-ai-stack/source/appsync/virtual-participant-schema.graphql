# Simple Virtual Participant Schema for Amplify Data
# This will auto-generate all CRUD operations, subscriptions, and resolvers

type VirtualParticipant @model @auth(rules: [{ allow: owner }]) {
  meetingName: String!
  meetingPlatform: String!
  meetingId: String!
  meetingPassword: String
  status: String!
}
